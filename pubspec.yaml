name: oggetto_app_hakathon
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  auto_route: ^5.0.3
  get_it: ^7.2.0
  provider: ^6.0.4
  json_annotation: ^4.7.0


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  auto_route_generator: ^5.0.1
  json_serializable: ^6.0.0
  build_runner: ^2.0.0

flutter:
  uses-material-design: true

